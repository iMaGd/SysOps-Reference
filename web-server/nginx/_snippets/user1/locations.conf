# Serve a health endpoint without server-side
location = /healthz { return 200 "ok\n"; }

# Extra cache rule for a public assets folder
location ^~ /assets/ {
    expires 7d;
    add_header Cache-Control "public, max-age=604800, immutable";
}
